<UserControl x:Class="FirstFloor.ModernUI.App.Pages.Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FirstFloor.ModernUI.App.Pages" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <DataGrid Name="DG1" ItemsSource="{Binding Customers}" AutoGenerateColumns="False" CanUserAddRows="False" AlternationCount="2" >
            <DataGrid.Columns>
                <mui:DataGridTextColumn Header="序号"  Binding="{Binding Code}"/>
                <mui:DataGridTextColumn Header="名字"  Binding="{Binding FirstName}" Width="50"/>
                <mui:DataGridTextColumn Header="姓" Binding="{Binding LastName}" />
                <mui:DataGridTextColumn Header="邮箱" Binding="{Binding Email}"/>
                <mui:DataGridCheckBoxColumn Header="成员" Binding="{Binding IsMember}" />
                <mui:DataGridComboBoxColumn Header="订单状态" SelectedItemBinding="{Binding Status}"  />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>