<UserControl x:Class="FirstFloor.ModernUI.App.Content.ControlsModernStepBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <Grid Style="{StaticResource ContentRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"  />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Margin="0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"  />
                <RowDefinition Height="9*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Row="0" Grid.Column="0" mui:ControlAttachProperty.Watermark="请输入要查询关键字" VerticalAlignment="Top"   />
        </Grid>

        <Border Grid.Row="1"  Background="#dddddd" Margin="0,0,0,0" >
            <StackPanel  Orientation="Horizontal"   VerticalAlignment="Center" HorizontalAlignment="Right"   >
                <Button  Content="确认" Command="{Binding SaveCommand}"   HorizontalAlignment="Center" Width="66" Height="28"   />
                <Button  Content="关闭" Command="{Binding Source={x:Static SystemCommands.CloseWindowCommand}}" Width="66" Height="28"  Margin="15,0,15,0"  HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
